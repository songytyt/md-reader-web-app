# Paper Markdown Reader

A local-first Markdown reader with a paper-like preview, generated outline, responsive one/two-page reading, and five-color saved highlights.

## Run locally

```bash
npm install
npm run dev
```

Open a Markdown file from the toolbar in a Chromium-based browser. Press `Cmd/Ctrl+S` to save highlights directly back to the original file.
